"""Model export for downstream serving.

Produces the artifacts consumed by the dregsbane-web-trail PWA's
on-device inference layer (see that repo's docs/on-device-inference-spec.md).
Each exporter is a CLI under this package.
"""
